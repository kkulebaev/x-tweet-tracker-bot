{
  "name": "x-tweet-tracker-bot",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Voyager — Telegram admin bot for x-tweet-tracker (talks to API only).",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "express": "^5.2.1",
    "grammy": "^1.41.1",
    "dotenv": "^17.3.1"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^25.5.0",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3"
  }
}
